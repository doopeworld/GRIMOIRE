# ---- build outputs (all regenerated by build_b70.sh / build_bridges_b70.sh) ----
bin/
*.so
*.o
*.a
runtime-b70/
b70_native_inference
gemv_small
launch_cost
argmax_cost
topk_cost
b70q4
# any bare compiled test/bench binary at repo root (no extension)
/drive_all
/drive_bestla

# ---- session artifacts / backups ----
*.tar.gz
*.tar.gz.sha256
*.log
*.bak
*.bak-*
*.bak2-*
*.pre-*
*.pre-*.NOTES
.backups-*/
work-*/

# ---- large data / models (never in git) ----
*.safetensors
*.b70
*.gguf
*.bin
*.pt
*.pth

# ---- os / editor ----
.DS_Store
*.swp
*~

# compiled bridge test binaries (extensionless ELF in src/)
src/test_xe2_gdn_bridge
src/test_xe2_bridge
src/test_xe2_attention_bridge
